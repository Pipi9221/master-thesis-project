# Issue 011/013 Re-check Notes (2026-03-09)

Environment:
- Ubuntu 22.04 (WSL)
- Frama-C 30.0 (Zinc)
- opam switch 4.14.2
- gcc

## Re-check result

- Issue 011 (`sizeof(...)` -> undeclared tmp): **NOT reproduced**
- Issue 013 (`sizeof(*p)` -> undeclared tmp): **NOT reproduced**

## Evidence from fresh artifacts

### 011
Current sliced output:
- `issues/011-sizeof-tmp-undeclared/artifacts/value_sizeof_buf_of_sizeof_struct.sliced.c`
- line 13: `uint8_t buf[sizeof(struct Pair)];`
- line 14: `sizeof(uint8_t [sizeof(struct Pair)])`

No `tmp_*` appears in artifacts under this issue directory.

### 013
Current sliced outputs (all variants) compile:
- `value_sizeof_deref_buf.sliced.c`
- `value_sizeof_index_buf.sliced.c`
- `value_sizeof_conditional_deref.sliced.c`
- `value_sizeof_addr_deref.sliced.c`

Representative file:
- `issues/013-sizeof-deref-tmp-undeclared/artifacts/value_sizeof_deref_buf.sliced.c`
- line 13: `uint8_t buf[sizeof(struct S)];`
- line 15: `sizeof(uint8_t [sizeof(struct S)])`

No `tmp_*` appears in artifacts under this issue directory.

## Compiler checks

All 011/013 artifacts compiled successfully with:

```bash
gcc -std=c11 -O0 -Wall -Wextra <artifact>.sliced.c
gcc -std=c11 -pedantic-errors -O0 -Wall -Wextra <artifact>.sliced.c
```

Only benign warnings remain (e.g. `unused variable 'buf'`).

## Source-path analysis

The historical root-cause chain documented in notes is technically plausible, and key code paths still exist:

1. Placeholder temp introduction in `cabs2cil.ml`:
- `src/kernel_internals/typing/cabs2cil.ml:1661-1680`
- creates temp var for `SizeOfE`-related contexts.

2. Local visibility filtering in slicing transform:
- `src/kernel_services/ast_transformations/filter.ml:519-520`
- keeps only `Info.loc_var_visible` locals.

3. Visibility source:
- `src/plugins/slicing/slicingTransform.ml:383-387`
- visibility is mark-based (`get_local_var_mark`).

4. Post-export normalization step:
- `src/plugins/slicing/slicingTransform.ml:450-468`
- `SizeOfE e'` -> `SizeOf t` when type is not variably-modified.
- invoked during export at `slicingTransform.ml:482`.

Interpretation:
- In plain `frama-c -print` outputs for these cases, temp vars still appear (e.g. `tmp_0` in `/tmp/issue011_print.c` and `/tmp/issue013_print.c`).
- In exported slicing artifacts, `SizeOfE` has been normalized to concrete type-based `SizeOf(...)`, so `tmp_*` references are eliminated before final output.
- Therefore the concrete compile-break symptom for 011/013 does not reproduce now.

## Recommendation

- Do not include 011/013 in the current one-shot submission as confirmed bugs.
- Keep them as historical/unconfirmed and reopen only if a fresh reproducer shows:
  1) exported `.sliced.c` still contains `sizeof(tmp_*)` or `sizeof(*tmp_*)`, and
  2) corresponding `tmp_*` declaration is absent, and
  3) gcc compile fails.
